- clean: ['~']

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - /usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"

- link:
    ~/.gitconfig: config/home/gitconfig
    ~/.oh-my-zsh/custom: config/home/oh-my-zsh/custom

- brewfile:
    file: Brewfile
    no-upgrage: true

- go:
    - package: github.com/StackExchange/dnscontrol
      flags: [-u]

# These links should be created after everything is installed:
- link:
    ~/.zshrc: config/home/zshrc